# Changelog

## 2.0.0

 - Switched configuration to YAML
 - Wire core `ErrorSchemaDescriber` into the schema describer chain
 - Resolve `info.version` `latest` to the installed `cowegis/cowegis-api-bundle` version
 - First phpspec coverage for `SchemaAction`
